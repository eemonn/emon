GUI is causing error in my code. It causes some exceptions and improper display. I am submitting my assignment with textUI.

There are four classes that implements interfaces:
1.MygameLogic implements gamelogic
2.Humanplayer implements Player,Human
3.AIplayer implements Player
4.TextUI implements UI (given)


** Main class to run the game.